server:
  port: 8002

spring:
  application:
    name: company-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/stockMarketApplication?createDatabaseIfNotExist=true
    username: root
    password: admin

  jpa:
    show-sql: true
    hibernate:
      ddl-auto = create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
